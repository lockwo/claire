# Slack Configuration (Required)
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
SLACK_SIGNING_SECRET=your-signing-secret

# LLM Providers (at least one required)
ANTHROPIC_API_KEY=sk-ant-your-key
OPENAI_API_KEY=sk-proj-your-key    # For OpenAI models (gpt-5.2, gpt-5-mini, etc.)
OPENROUTER_API_KEY=sk-or-your-key  # Optional - for other providers via OpenRouter

# Default model (uncomment one based on your LLM provider)
# DEFAULT_MODEL=claude-sonnet-4-5-20250929  # For Anthropic (Claude 4.5)
# DEFAULT_MODEL=gpt-5.2                      # For OpenAI

# GitHub (Optional - for private repo access)
GITHUB_TOKEN=ghp_your-personal-access-token

# Storage Options
# For development, local file storage is used by default
USE_LOCAL_STORAGE=true
LOCAL_DATA_DIR=./data

# For production with PostgreSQL (optional)
# DATABASE_URL=postgres://user:password@localhost:5432/claire

# For production with GCS artifact storage (optional)
# GCS_BUCKET=claire-artifacts
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Runtime Configuration (Optional)
# NODE_ENV=development
# LOG_LEVEL=info           # debug, info, warn, error
# LOG_FORMAT=text          # text (human readable) or json (for log aggregation)
# LOG_DIR=./logs           # directory for log files (default: ./logs)
# WORKER_IDLE_TIMEOUT_MS=600000
# WORKER_MAX_RUNTIME_MS=1800000
# MAX_AGENT_ITERATIONS=50
